/**
 * Shared Veo 3.1 brand-film prompt, prepended to every generated clip so the whole video
 * library reads as one cohesive brand film. Consumed by data/veo-frames.ts.
 */

export const SHARED_BRAND_PROMPT =
  "Cinematic defense-tech product film. Matte near-black studio void, single hard key light " +
  "with a deep red rim accent, volumetric haze, slow deliberate camera moves, shallow depth of " +
  "field, 24fps filmic motion. Precision-instrument mood — controlled, premium, never flashy. " +
  "No text, no logos, no people.";
